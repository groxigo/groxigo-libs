/**
 * @groxigo/ui-elements-web
 *
 * Primitive UI building blocks for web applications using React + CSS Modules.
 * API-compatible with @groxigo/ui-elements (React Native) for consistency.
 *
 * Usage:
 * ```typescript
 * import { Text, Button, Card } from '@groxigo/ui-elements-web';
 *
 * <Text variant="h1" colorScheme="primary">Hello</Text>
 * <Button colorScheme="primary" onPress={() => {}}>Click me</Button>
 * ```
 */

// Elements
export * from './elements';

// Utilities
export { cn } from './utils';
