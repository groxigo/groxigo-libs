/**
 * @groxigo/ui-core
 *
 * Shared UI utilities, hooks, and animation configs for cross-platform components.
 *
 * Usage:
 * ```typescript
 * import { useDisclosure, useDebounce, cn } from '@groxigo/ui-core';
 * import { timingPresets, springPresets } from '@groxigo/ui-core/animation';
 * import { useControllable } from '@groxigo/ui-core/hooks';
 * ```
 */

// Re-export hooks
export * from './hooks';

// Re-export animation utilities
export * from './animation';

// Re-export utilities
export * from './utils';
