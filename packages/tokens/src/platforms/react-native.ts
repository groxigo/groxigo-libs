/**
 * React Native Platform Export
 * 
 * Exports tokens for React Native platform (iOS, Android, Web via RN Web).
 * These are JavaScript objects compatible with StyleSheet.
 */

export * from '../tokens';

// Re-export for convenience
import { tokens } from '../tokens';
export default tokens;

