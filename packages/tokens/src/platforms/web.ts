/**
 * Web Platform Export
 * 
 * Exports tokens for web platform (CSS variables).
 * This file can be imported to get CSS variable names.
 */

export * from '../tokens';

// Re-export for convenience
import { tokens } from '../tokens';
export default tokens;

